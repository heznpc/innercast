interface:
  display_name: "Innercast"
  short_description: "Stable inner characters for same-task AI decisions"
  default_prompt: "Use $innercast on this decision. Run the named inner characters in the current task, wait for their positions, then have the host make the final decision."
