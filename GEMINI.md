# Innercast development

Read `AGENTS.md` and run `npm run harness:context` from the repository root
before working. `harness/product.json` records the native-pet product intent;
`harness/handoff.json` records implementation status and the next action.
Existing advisory agents and browser previews are not native-pet integration.
